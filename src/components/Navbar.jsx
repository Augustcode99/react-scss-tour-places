import navStyle from "../scss/navbar.module.scss";

const Navbar = () => {
  return (
    <nav>
      <a href="#">ABOUT US</a>
      <a href="#">DEALS</a>
      <a href="#">SERVICES</a>
      <a href="#">ACTIVITIES</a>
      <a href="#">BLOG</a>
      <a href="#">CONTACT</a>
    </nav>
  );
};

export default Navbar;
